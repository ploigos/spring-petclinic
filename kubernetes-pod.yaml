apiVersion: v1
kind: Pod
spec:
    serviceAccount: jenkins
    containers:
    - name: 'jnlp'
      image: "ploigos/ploigos-ci-agent-jenkins:latest"
      tty: true
      volumeMounts:
      - mountPath: '/home/ploigos'
        name: home-ploigos
    - name: 'default'
      image: 'quay.io/dwinchell_redhat/ploigos-and-tools:latest'
      tty: true
      volumeMounts:
      - mountPath: '/home/ploigos'
        name: home-ploigos
    volumes:
    - name: home-ploigos
      emptyDir: {}

